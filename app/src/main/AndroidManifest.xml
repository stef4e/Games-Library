<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/games_library"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/games_library_rounded"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamesLibrary"
        tools:targetApi="31">
        <activity android:name=".AuthActivities.LoginActivity" />
        <activity android:name=".AuthActivities.SignUpActivity" />
        <activity
            android:name=".Gameplay.GamesSearchActivity"
            android:windowSoftInputMode="adjustPan">
        </activity>
        <activity android:name=".Gameplay.GamesLibraryActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>